{
  "mcpServers": {
    "frontleap-admin": {
      "url": "${FRONTLEAP_URL}/mastra/api/mcp/admin/mcp",
      "auth": {
        "CLIENT_ID": "${OAUTH_CLIENT_ID}",
        "CLIENT_SECRET": "${OAUTH_CLIENT_SECRET}"
      }
    }
  }
}
